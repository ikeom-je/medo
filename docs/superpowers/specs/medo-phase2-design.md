# Medo フェーズ2 設計(索引)

ステータス: レビュー中(3エージェント相互検証を5ラウンド実施済み。実装計画化の前にユーザー承認が必要)

本ファイルは**目的・不変条件・標準周回・主要判断**と各詳細への索引を持つ。詳細の正本は分割先のファイルにあり、本ファイルは要約とリンクに留める(二重管理を避ける)。

| 詳細 | 内容 |
|---|---|
| [ドメインモデル](phase2-domain-model.md) | 要件ドキュメントのスキーマ。現状・あるべき姿・GAP・真因・課題・仮説 |
| [ワークフローモデル](phase2-workflow-model.md) | 進行記録のイベント、畳み込み、収束規則 |
| [status契約](phase2-status-contract.md) | 診断の階層、projection、JSON契約 |
| [生成物のライフサイクル](phase2-artifact-lifecycle.md) | 依存グラフ、陳腐化、カバレッジ |
| [スライド設計](phase2-slides-design.md) | AsIs説明スライド / 最終提案スライド |
| [Skill構成と移植性](phase2-skill-portability.md) | Skillの分割、どのAgentでも動かすための条件 |

---

## 1. 目的

フェーズ1は「What/Whyの合意形成」の縦切りを通した。フェーズ2は、その合意形成を**利用者が漏れなく重複なく進められるよう導く**支援に踏み込む。

**medoの位置づけ**: 利用者を導く支援ツールであり、判断を代行しない。フレームワークをパターンとして整理し、MECE(漏れなく重複なく)なヒアリングを支える。

---

## 2. 不変条件

フェーズ2の設計変更で**変えてはならない**もの。

1. **数値・事実の通り道にLLMを挟まない**(product.md 原則1)。保存済みの値の参照・計算・鮮度判定はコードが行う
2. **課題も要件も最初から確定しない**(原則3)。確度は `confidence` で表し、進行制御(ゲート)で表さない
3. **自律的なmulti-agentを採らない**(差別化軸)。会話Agentは1体(ホストLLM)+決定論的ツール群
4. **medoはコマンドラインツールである**。Skillは手順書であり、実行主体は常にホストLLM。medo自身はAgentにならない
5. **認証・マルチテナントを導入しない**(利用スコープは本人のみ)
6. **診断は報告であって強制ではない**。未接続・未確認・未解決を検出しても保存を拒否しない
7. **厳密さは事実の層に、寛容さはプロセスの層に**。出典必須・数値の通り道にLLMを挟まない・鮮度判定は一切緩めない。一方、チェックが埋まらなくても・矛盾が残っていても**進行は止めない**。これを混同すると「一緒に考える」が「根拠なく描く」に滑る

---

## 3. 標準周回(4ステージ)

暗黙知は**AsIsとToBeを何度も往復して初めて出てくる**。往復は頭の中では回らず、**各ステージで成果物を出し、レビューし、顧客にぶつけて反応を得る**ことで進む。

```
┌─────────────────────────────────────────────────┐
│  1. Investigate & Draft   調べる・仕立てる        │
│     公開情報と顧客情報を集め、現状を整理して出す   │
│                      ↓                          │
│  2. Internal Review       内部検証               │
│     論理矛盾・GAP・欠落を洗い出し論点を研ぐ        │
│                      ↓                          │
│  3. Client Dialogue       ぶつける・反応を得る     │
│     顧客に共有し、確認・共感・異議を記録する        │
│                      ↓                          │
│  4. Adapt & Decide        振り返る・次へ進む       │
│     反応を要件に反映し、往復継続か次段階かを判断    │
└──────────────────┬──────────────────────────────┘
                   └──▶ 1へ戻る(収束するまで)
```

**これは固定の状態機械ではなく「標準的な1周」である**。利用者がどのステージから始めても成立する(判断2)。順序を強制せず、CLIは現在地だけを返す。

**当初案の7ステップから4ステージへ集約した**(相互レビュー)。成果物出力や内部フラグ操作まで分解すると、実務者が記憶して回すには粒度が細かすぎる。日常的に意識できるのは3〜4が限界である。

### ステージとコンポーネントの対応

| Stage | 主な入力 | 保存するもの | 主なCLI操作 | 次の判断材料 |
|---|---|---|---|---|
| **1. Investigate & Draft** | 公開情報・顧客の生の声・提供資料 | `facts` / `RequirementsDoc.as_is` / `research` / `as-is-report` / **討議用 `slides`** | `facts save` / `requirements save` / `artifacts save` | `model.coverage` |
| **2. Internal Review** | 最新の `as-is-report` **と討議用スライド** | `AsIsReportReviewed` イベント | `medo review add` | `workflow.review` |
| **3. Client Dialogue** | レビュー済みの討議用スライド | `StakeholderResponded` イベント | `medo respond add` | `workflow.responses` |
| **4. Adapt & Decide** | レビュー所見・顧客の反応 | 要件の新版 / `ToBeCheckpointRecorded` イベント | `requirements save` / `medo checkpoint answer` | `readiness` / `actions` |

**討議用スライドはステージ1で作る**。顧客に投影する資料こそリフレーミング規約が課される対象であり、ステージ2の内部レビューを通してから提示する。当初案はステージ3で生成しており、レビューを素通りしていた。

各ステージは `medo status` の該当ブロックだけを読めば進められる(詳細: [status契約](phase2-status-contract.md))。

### 周回ごとに検証論点を1つ定める

**何を検証すれば仮説が前進・棄却するかを事前に決める**。周回ごとに `focus_hypothesis_id` を1つ選び、その周回の顧客対話はそれを検証することに集中する。

これが無いと、蓄積した課題とGAPすべてに一律で向き合うことになり、周回が発散する。Lean Startupのbuild-measure-learnが実験前に検証基準を定めるのと同じ考えである([The Lean Startup](http://theleanstartup.com/principles))。

### 回ること自体が価値である

**チェックリストが埋まらなくても、周回を重ねること自体が解像度を上げる**。顧客はAsIsはもちろん、ToBe(PRFAQ/PRD)も描けていない可能性が高い。medoは埋まっていない項目を数える道具ではなく、**一緒に描いていく道具**である。

周回ごとに `round_delta`(今回新たに分かったこと)を返し、進んでいることを可視化する。「顧客があるべき姿を語れないと分かった」ことも成果として数える。

---

## 4. 主要な設計判断

詳細な根拠は各詳細ファイルにある。ここでは判断とその一行理由のみを置く。

| # | 判断 | 理由 | 詳細 |
|---|---|---|---|
| 1 | フレームワークに基づく**固定スキーマ**を持つ(汎用ノード+タグにしない) | 項目が定義されていなければ「何が漏れているか」を機械的に指摘できず、MECEを担保できない | [domain](phase2-domain-model.md) |
| 2 | **順序は固定しない**。CLIが充足状況を返し、Skillが次の問いを決める | 実案件は典型パターンに従わない。理想像から語り始めることも、制約から入ることもある | [status](phase2-status-contract.md) |
| 3 | **顧客に問う項目とSkillが下書きする項目を分ける** | 顧客はGAPと真因の区別に沿って話さない。空欄を機械的に埋めようとすると穴埋め尋問になる | [domain](phase2-domain-model.md) |
| 4 | **AsIsとToBeを往復させて暗黙知を引き出す**。ゲートを設けない | 仮のToBeは実態を引き出すプローブ。AsIsの薄さはToBeを止める理由ではなく確度を `assumed` に留める理由 | [workflow](phase2-workflow-model.md) |
| 5 | 往復は**出力→レビュー→共有→反応**で回す | 往復は頭の中では回らない。成果物と反応がないと暗黙知は出てこない | [workflow](phase2-workflow-model.md) |
| 6 | 要素間の**リンクは任意**。未接続はエラーにしない | 痛みは明確だが理想像が未言語化、という状態は実案件で普通に起きる | [domain](phase2-domain-model.md) |
| 7 | **解像度は `confidence` が制御する** | 知らない顧客情報は仮説。スキーマを粗くするのではなく確度で表す | [domain](phase2-domain-model.md) |
| 8 | **GAP(現象)とボトルネック(真因)を分離する** | 現象の差分をそのまま課題と呼ぶと対症療法しか出ない | [domain](phase2-domain-model.md) |
| 9 | **未検証の真因は仮説に一元管理する** | 確定した真因と未検証の仮説を二重に持つと整合が崩れる | [domain](phase2-domain-model.md) |
| 10 | **進行記録は要件と分けてイベントとして持つ** | 要件は保存のたびに版が進むため、反応やレビューを要件内に置くと記録した瞬間に旧版宛てになる | [workflow](phase2-workflow-model.md) |
| 11 | **収束判断は「現在の対象」に対してのみ行う** | 旧版への異議で永久に止まる、古い合意で誤って通る、の両方を防ぐ | [workflow](phase2-workflow-model.md) |
| 12 | **サブエージェント機構に依存しない** | Claude CodeのTask相当がCodex/agyに無い。Skill+CLIだけで「モデルを変えて実行する」を実現する | [portability](phase2-skill-portability.md) |
| 13 | **未確定・矛盾・判断不能は課題の候補として扱う** | 顧客が理想像を語れないことは欠落ではなく発見でありうる。矛盾やトレードオフ自体が解くべき課題であることもある | [workflow](phase2-workflow-model.md) |
| 14 | **チェックリストの正本はCLIに置き、文書へ投影する** | 文書に埋め込むと更新が分散し、記録が本文に埋まってCLIが検出できない。文書ごとに別の観点を持ち、段階的に出す | [workflow](phase2-workflow-model.md) |
| 15 | **診断の主役は `readiness` ではなく `actions`** | 「足りない」の列挙は監査人の視点。伴走ツールは「次にできること」を先に示す | [status](phase2-status-contract.md) |

---

## 5. フェーズ2の実装順序

**依存が閉じる順に並べる**。各項目は先行項目の成果だけで着手できる。

| 優先度 | 項目 | 依存が閉じる理由 |
|---|---|---|
| **1** | ドメインモデル + ID規約 + 採番簿 + 変更manifest + 移行 | 他のすべての土台。単独で着手できる |
| **2** | 生成物の型定義(`research` / `as-is-report`)+ 依存グラフ + stale伝播 + カバレッジ | 1のmanifestを使う。**イベントが `as-is-report` を対象にするため4より先** |
| **3** | ワークフローイベント + 収束規則 | 2の `as-is-report` を対象にできる |
| **4** | `model` / `workflow` / `readiness` / `actions` 診断 | 1〜3のデータが揃って初めて合成できる |
| **5** | Skill 4本 + 討議用スライド生成 | 4の `actions` を読んで動く。討議用スライドはステージ1の成果物なので同時 |
| **6** | 最終提案スライド + `phase_signoff` ゲート | 5の後。`prfaq` が前提 |
| 並行 | 出典検証の強化(URLフェッチ + 数値突合) | **他と技術的に独立**。いつ着手してもよい |
| 後続 | ナレッジ来歴 / `knowledge-digest` / `decision-roadmap` / `build-mock` / `propose-architecture` / pricing / 簡易Webアプリ | **詳細設計が未了**。下記 |

**当初案は依存が閉じていなかった**。「イベント基盤(4)をSkill(6)より先」は正しいが、**イベントの対象である `as-is-report` の型定義が6に含まれていた**ため、4だけでは収束規則もレビューも実装できなかった。また6を「標準周回を回せる最小構成」としながらスライド生成が7にあり、ステージ3が成立しなかった。

### 詳細設計が未了の項目

次の項目は**実装順序に名前があるだけで詳細正本を持たない**。着手前に設計ドキュメントを起こす。

| 項目 | 未了の内容 |
|---|---|
| 出典検証の強化 | URLフェッチの失敗時の扱い、数値突合のアルゴリズム |
| ナレッジ来歴 / `knowledge-digest` | `supersedes` のスキーマ、旧引用の解決規則、統合提案の入出力 |
| `decision-roadmap` | 出力形式(生成物typeを新設するか `comparison` を使うか)が未決 |
| pricing(再定義) | クラウド非依存でのゴールデンデータの持ち方 |
| `build-mock` | 合意形成用モックの生成物type・保存形式 |
| `propose-architecture` | 完全版PRFAQの技術的背景との関係、出力形式 |
| 簡易Webアプリ | coreをどう公開するか(CLIとの責務分担) |

### 相互レビューで訂正された当初案の誤り

- **pricing計算機の削除は誤り**: 「クラウド非依存」は「料金を扱わない」ことを意味しない。不正だったのはテスト方針(単一の公式Calculatorを正解に置けない)であり機能の要否ではない。**再定義して後送り**
- **簡易Webアプリの除外根拠が誤り**: 正本 `medo-design.md` はフェーズ2に明記しており、差別化軸の「単一のWebアプリに閉じる」は「Webアプリを作らない」という意味ではない。**優先度を下げるが除外しない**

### フェーズ2完了定義の改訂(要承認)

現行は「課題→What/Why合意→スライド+モックまで半日」だが、`build-mock` を後続に送るため改訂が必要:

> **改訂案**: 課題→標準周回による現状と理想の合意(収束判定を満たす)→共感できるドキュメント+提案スライドまで半日。knowledgeが案件を跨いで洗練される。

---

## 6. 未決事項

1. 重複検知(MECEのE)を `knowledge-digest` と実装共有するか、別機能にするか
2. 差別化の訴求(証跡追跡可能性)を正本のどこに書くか。**競合ツールの具体的な弱点は未検証のため、出典なしにドキュメントへ書かない**
3. `decision-roadmap` の出力形式(生成物typeを新設するか、既存の `comparison` を使うか)
4. **Strawman提示のバイアス誘導リスクへの対処**(未解決)。仮説の精度が低いと初期信用を失い、顧客が迎合的だと誤った仮説が固定化される。撤退基準は未定
5. **進行記録の状態をSkillが会話ログから正確に判定・更新できるか**(未検証)。実案件で検証する
